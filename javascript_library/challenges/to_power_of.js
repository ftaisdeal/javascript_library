// Create a function that takes a base number and an exponent number and returns the calculation.

// Solutions

function calculateExponent1(num, exp) {
  return Math.pow(num, exp);
}


function calculateExponent2(num, exp) {
  return num ** exp
}


const calculateExponent3 = (num, exp) => Math.pow(num, exp);