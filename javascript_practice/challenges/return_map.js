// Write a function that maps an array to return an array with each element
// in the original array multiplied by 2

// Solutions

function getMultipliedArr1(arr) {
  return arr.map(num => num * 2)
}


const getMultipliedArr2 = (arr) => arr.map(el => el * 2);